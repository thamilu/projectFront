const code = require('fs').readFileSync('app/globals.css', 'utf8'); let stack = []; let inC = false; let l = 1; for (let i = 0; i < code.length; i++) { if (code[i] === '\n') l++; if (!inC && code[i] === '/' && code[i+1] === '*') { inC = true; i++; continue; } if (inC && code[i] === '*' && code[i+1] === '/') { inC = false; i++; continue; } if (!inC) { if (code[i] === '{') stack.push(l); if (code[i] === '}') { if (stack.length === 0) console.log('Extra } at line', l); else stack.pop(); } } } console.log('Unclosed { at lines:', stack);
